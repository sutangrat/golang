package main

import (
	"fmt"
	"net"
	"net/rpc"
)

type ExampleStruct struct{}

func (this *ExampleStruct struct) Plus (args []float64,rely *float64) erorr{
	sum := 0.0
	for _, v := range args {
		
	}
}